- today = Date.today
- @date ||= today
- content_for :sidebar do
  %ul.summary
    %li.new-content
      =sidebar_new_content_link

%h2 Users

.clear
= render :partial => 'admin/common/list_table', :locals => { :records => @collection,	:class_obj => User, :list_cols => %w(name email), :action_cols => %w() }