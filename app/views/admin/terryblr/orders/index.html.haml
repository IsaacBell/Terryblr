= render :partial => 'sidebar'

= render :partial => 'admin/common/search_form'

%h2 Orders

.clear
= render :partial => 'admin/common/list_table', :locals => { :records => @orders,	:class_obj => Order, :list_cols => %w(order buyer items date total), :action_cols => %w() }