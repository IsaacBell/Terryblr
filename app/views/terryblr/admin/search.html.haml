%h2 Search results for '#{@query}'

- %w(posts products pages).each do |key|
  - records = @results[key.to_sym]
  - span = key=="pages" ? 5 : 6
  %div{ :class => "search-results span-#{span}", :id => "#{key}_results" }
    %h3=pluralize records.total_entries, key.singularize.humanize
    - if records.empty?
      %p No results found
    - else
      = render :partial => 'admin/common/archives', :locals => { :records => records }
